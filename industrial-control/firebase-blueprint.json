{
  "entities": {
    "Machine": {
      "title": "Machine",
      "description": "Represents a machine inside the Assembly Lab layout.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "description": "The type of machine (e.g., cnc, robot, conveyor)"
        },
        "positionX": {
          "type": "number",
          "description": "X coordinate in the 3D space"
        },
        "positionY": {
          "type": "number",
          "description": "Y coordinate in the 3D space"
        },
        "positionZ": {
          "type": "number",
          "description": "Z coordinate in the 3D space"
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this machine"
        }
      },
      "required": ["type", "positionX", "positionY", "positionZ", "userId"]
    }
  },
  "firestore": {
    "/users/{userId}/machines/{machineId}": {
      "schema": {
        "$ref": "#/entities/Machine"
      },
      "description": "Stores the machines added by the user to their factory layout."
    }
  }
}
